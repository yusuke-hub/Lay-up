<h4>グループページ</h4>
	<div class="gf  mt-3">
    	<%= form_for(@group) do |f| %>
    		<%= f.label :グループ名 %><br />
    		<%= f.text_field :name %><br />
    		<%= f.label :メモ %><br />
    		<%= f.text_area :caption %><br />
    		<%= f.submit "作成",class:'btn btn-sm btn-success mt-2' %>
    	<% end %>
</div>