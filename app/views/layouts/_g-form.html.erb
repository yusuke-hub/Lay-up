<%= form_for(group) do |f| %>
  <%= f.label :グループ名 %><br />
  <%= f.text_field :name %><br />
  <%= f.label :説明 %><br />
  <%= f.text_area :caption %><br />
  <%= f.submit "作成",class:'btn btn-sm btn-primary' %>
<% end %>